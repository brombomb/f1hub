{
  "name": "f1-hub",
  "version": "0.0.0",
  "devDependencies": {
    "browser-sync": "^3.0.4"
  },
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "start": "browser-sync app -w --port 9000"
  }
}
